{
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=portal-app",
    "dev": "turbo run dev --parallel",
    "sb": "turbo run dev --filter=component-library",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "portal": "turbo run dev --parallel --filter=portal-app"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@7.15.0"
}
